{
    "name": "n8n Credentials Template",
    "version": 1,
    "description": "Template de credenciais para importar no n8n. Edite os valores antes de importar.",
    "credentials": [
        {
            "name": "Chatwoot API",
            "type": "httpHeaderAuth",
            "data": {
                "name": "api_access_token",
                "value": "{{$env.CHATWOOT_API_TOKEN}}"
            },
            "metadata": {
                "description": "API Token do Chatwoot para envio de mensagens"
            }
        },
        {
            "name": "PostgreSQL TikTrend",
            "type": "postgres",
            "data": {
                "host": "{{$env.N8N_DB_POSTGRESDB_HOST}}",
                "port": 5432,
                "database": "{{$env.N8N_DB_POSTGRESDB_DATABASE}}",
                "user": "{{$env.N8N_DB_POSTGRESDB_USER}}",
                "password": "{{$env.N8N_DB_POSTGRESDB_PASSWORD}}",
                "ssl": false
            },
            "metadata": {
                "description": "Conexão com banco PostgreSQL principal"
            }
        },
        {
            "name": "Redis TikTrend",
            "type": "redis",
            "data": {
                "host": "{{$env.REDIS_HOST}}",
                "port": 6379,
                "password": "{{$env.REDIS_PASSWORD}}",
                "database": 0
            },
            "metadata": {
                "description": "Conexão Redis para cache e rate limiting"
            }
        },
        {
            "name": "OpenAI TikTrend",
            "type": "openAiApi",
            "data": {
                "apiKey": "{{$env.OPENAI_API_KEY}}"
            },
            "metadata": {
                "description": "API Key OpenAI para chatbot IA"
            }
        },
        {
            "name": "Evolution API",
            "type": "httpHeaderAuth",
            "data": {
                "name": "apikey",
                "value": "{{$env.EVOLUTION_API_KEY}}"
            },
            "metadata": {
                "description": "API Key Evolution para WhatsApp"
            }
        },
        {
            "name": "Backend TikTrend API",
            "type": "httpHeaderAuth",
            "data": {
                "name": "Authorization",
                "value": "Bearer {{$env.DIDIN_API_TOKEN}}"
            },
            "metadata": {
                "description": "Token de autenticação para backend TikTrend"
            }
        }
    ],
    "instructions": {
        "pt-BR": [
            "1. Copie o arquivo .env.n8n.example para .env.n8n",
            "2. Preencha todas as variáveis de ambiente",
            "3. No n8n, vá em Settings → Credentials",
            "4. Para cada credencial acima:",
            "   - Clique em 'Create New'",
            "   - Selecione o tipo correspondente",
            "   - Preencha os campos usando as variáveis de ambiente",
            "   - Salve com o nome exato mostrado acima",
            "5. Importe os workflows",
            "6. Os workflows usarão automaticamente as credenciais"
        ],
        "security_notes": [
            "⚠️ NUNCA commite o arquivo .env.n8n no git",
            "⚠️ Use secrets manager em produção (AWS Secrets, Vault, etc)",
            "⚠️ Rotacione tokens regularmente",
            "⚠️ Use HTTPS em produção"
        ]
    }
}